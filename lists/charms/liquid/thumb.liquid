{% assign props = item.props %}

{% if props.thumb %}
	{% assign thumb = props.thumb %}
{% else %}
	{% assign thumb = props.img %}
{% endif %}

<image src="{{thumb.src}}" width="{{thumb.width | divided_by: 2}}" height="{{thumb.height | divided_by: 2}}"/>
